import { StoryObj, Meta } from '@storybook/react'
import { StarList } from '@bookwise-ui/react'

export default {
    title: "Data display/Star",
    component: StarList,
    args: {
    }
} as Meta

export const Primary: StoryObj = {}

